import { ProgressiveContent, SectionBreak } from '../components/mdx/ProgressiveContent';
import { BulletList, Definition, ExampleBlock, KeyTakeaways, PageHeader, Concept, Alert } from '../components/mdx/educational-components';
import SampleSpacesEvents from '../components/01-introduction-to-probabilities/1-1-1-SampleSpacesEvents';
import ConceptSection from '../components/shared/ConceptSection';

<ProgressiveContent>

<PageHeader 
  title="Sample Spaces and Events"
  subtitle="Chapter 1, Section 1"
/>

Let's start with a fundamental question: How do we mathematically describe all possible outcomes of an experiment?

<SectionBreak />

## What is a Sample Space?

In probability theory, we use a **sample space** to represent all possible outcomes.

<Definition term="Sample Space">
  A sample space $S$ is the set of all possible outcomes of a random experiment.
</Definition>

<SectionBreak />

## Examples

Let's look at some examples:

<BulletList color="blue">
  <li><strong>Coin flip:</strong> $S = \{\text{Heads}, \text{Tails}\}$</li>
  <li><strong>Die roll:</strong> $S = \{1, 2, 3, 4, 5, 6\}$</li>
  <li><strong>Two coin flips:</strong> $S = \{HH, HT, TH, TT\}$</li>
</BulletList>

<Alert type="info">
  Notice how the sample space depends on the experiment!
</Alert>

<SectionBreak />

## What are Events?

<Concept title="Events" icon="🎯">
  An **event** is any subset of the sample space. It represents a collection of outcomes we're interested in.
</Concept>

For example, when rolling a die:

<BulletList color="purple">
  <li>Event $A$: "Rolling an even number" = $\{2, 4, 6\}$</li>
  <li>Event $B$: "Rolling less than 3" = $\{1, 2\}$</li>
</BulletList>

<SectionBreak />

## Combining Events

We can combine events using set operations:

<ExampleBlock 
  color="blue"
  title="Union ($A \cup B$)"
  description="Either A or B occurs"
>
  Example: $\{2, 4, 6\} \cup \{1, 2\} = \{1, 2, 4, 6\}$
</ExampleBlock>

<ExampleBlock 
  color="green"
  title="Intersection ($A \cap B$)"
  description="Both A and B occur"
>
  Example: $\{2, 4, 6\} \cap \{1, 2\} = \{2\}$
</ExampleBlock>

<ExampleBlock 
  color="red"
  title="Complement ($A'$)"
  description="A does not occur"
>
  Example: $\{2, 4, 6\}' = \{1, 3, 5\}$
</ExampleBlock>

<SectionBreak />

## Interactive Visualization

Now let's explore these concepts interactively. Use the Venn diagram below to see how different set operations work.

Try expressions like:

<BulletList color="green" icon="arrow">
  <li><code className="bg-neutral-800 px-2 py-1 rounded">A∪B</code> (A union B)</li>
  <li><code className="bg-neutral-800 px-2 py-1 rounded">A∩C</code> (A intersect C)</li>
  <li><code className="bg-neutral-800 px-2 py-1 rounded">(A∪B)'</code> (complement of A union B)</li>
</BulletList>

<ConceptSection
  title="Interactive Venn Diagram"
  description="Explore set operations visually"
>
  <SampleSpacesEvents />
</ConceptSection>

<SectionBreak />

<KeyTakeaways>
  <li>A sample space contains all possible outcomes</li>
  <li>Events are subsets of the sample space</li>
  <li>We can combine events using union, intersection, and complement</li>
</KeyTakeaways>

Ready to move on? Next, we'll learn about counting techniques to calculate the size of sample spaces and events.

</ProgressiveContent>